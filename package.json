{
  "name": "it-news-infoscreen",
  "private": true,
  "version": "1.0.0",
  "description": "Production-ready IT news infoscreen for office TVs",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "generate:data": "node scripts/generate-static-news.mjs",
    "build": "npm run generate:data && npm run build --prefix frontend",
    "build:web": "npm run build",
    "start": "npm run start --prefix backend",
    "package:win": "node build-exe.js"
  },
  "type": "module",
  "devDependencies": {
    "@yao-pkg/pkg": "^6.14.1",
    "concurrently": "^9.2.1",
    "esbuild": "^0.27.3"
  }
}